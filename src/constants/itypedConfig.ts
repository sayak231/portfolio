import { personalData } from "./personal-data";

export const itypedConfig = Object.freeze({
  showCursor: true,
  typeSpeed: 100,
  backDelay: 1000,
  backSpeed: 60,
  loop: true,
  strings: personalData.intro.itypedStrings,
});
